{
  "name": "common",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean": "del-cli ./build/*",
    "build": "npm run clean && npx tsc",
    "pub": "npm run build && git add . && git commit -m \"Updates\" && npm version patch -m \"Release version %s\" && git push origin main --tags"
    },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "del-cli": "^6.0.0",
    "typescript": "^5.6.3"
  }
}
